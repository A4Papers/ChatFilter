ChatFilter:
  advert:
    Enabled: true
    Warn:
      Staff: true
      Player: true
      Console: true
    Regex: \b[0-9]{1,3}(\.|dot|\(dot\)|-|;|,|(\W|\d|_)*\s)+[0-9]{1,3}(\.|dot|\(dot\)|-|;|,|(\W|\d|_)*\s)+[0-9]{1,3}(\.|dot|\(dot\)|-|;|,|(\W|\d|_)*\s)+[0-9]{1,3}\b
    CancelChat: true
    ReplaceWith: Cookies
    Command:
    AddedBy: ChatFilter
  advert1:
    Enabled: true
    Warn:
      Staff: true
      Player: true
      Console: true
    Regex: ([a-zA-Z0-9\-\.]+\s?(\.|dot|\(dot\)|-|;)(com|org|net|cz|co|uk|sk|biz|mobi|xxx|eu|io))
    CancelChat: true
    ReplaceWith: Cookies
    Command:
    AddedBy: ChatFilter

